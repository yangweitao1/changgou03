+ 回顾下oauth2.0
+ 微服务需要保护()
+ 网关对接微服务(oauth2.0)
+ 动态的加载数据
    + client的数据(changgou)
    + 用户的数据(szitheima)
+ 购物车的功能
    + 思路
    + 代码实现

+ feign的调用 传递头信息 (解决)

+ 每一个微服务可能需要用户的数据(获取用户的数据)


+ oath2.0
    + 实现单点登录  (用户在使用畅购系统的时候,只需要登录一次,就可以访问其他的相互信任的系统,利用token)
    + 实现第三方认证 ()
    + 微服务之间的安全调用 (用户微服务 )

+ 四个角色
    + 用户(资源拥有者)
    + 授权服务器(认证服务器)
    + 客户端 (client)
    + 资源服务器()

+ 授权模式
    + 授权码>>
    + 密码模式>>
    + 隐式模式
    + 客户端模式


+ 认证服务器 需要动态的加载 数据库changgou_user 库中的tb_user表中的密码数据

+ 认证服务器接收用户的请求,
+ 调用user的微服务的feign 根据用户的名称 查询用户的密码
+ 匹配认证.


+ 购物车
	+ 需要先登录,才能添加购物车
	+ 添加购物车
		+ 请求:/cart/add
		+ 参数:sku的ID, num
		+ result
		
		controller servcie dao


+ 熔断器
	+ 后备模式(fallback) 服务降级
	+ 壁舱模式(thread隔离) 默认的开启
	+ 信号量模式(SEMAPHORE)	 共享线程
	+ 负载均衡

+ 用户名获取:
	+ 微服务校验通过
	+ 使用springscurity获取认证信息
	+ 解析令牌
	+ 获取到令牌中的用户名