作业:
	抽取所有的通用的controlelr service 

	实现CRUD  包括分页.


	+ 反射 (举例子)
	+ 抽象类 (举例子)
	+ 接口


+ 实现页面登录

+ 下单
	+ 下订单
	+ 减库存
	+ 用户积分增加


 +支付的流程

 +二维码生成


 + 页面登录
	+ 用户发送请求
	+ thymeleaf来渲染页面给用户
	+ 用户输入用户名和密码
	+ 生成令牌就返回


 + vue实现登录
	+ js引入
	+ 编写一个method
	+ post请求

+ 显示用户的地址列表
	+显示当前登录的用户的地址列表  
	
	思路:
		前端发送请求给后台
		后台接收到请求之后,获取当前登录的用户名
		根据当前的登录用户名查询该用户下的所有的地址列表
		返回给前端
		controller servcie dao


+ 提交订单
	+添加订单表记录

	+添加订单选项表记录

	+减少SKU的库存
		update tb_sku set num=num-#{num},sale_num=sale_num+#{num} where id=#{skuId} and num>=#{num}
	+增加用户的积分
		update tb_user set points=points+#{points} where username=#{username}
	+清空当前登录的用户的购物车数据
	

	思路:
		用户点击结算 发送请求
		后台微服务(订单微服务)接收请求
		提交订单(添加订单表 添加订单选项表的记录,....)
		返回给前端 result


 + 二维码(QR Code)
	+ 一维条形码 (商品扫码枪) 只能数字 存的数据30个数字. 
	+ 二维条形码(二维码) (存储数据量大 500个汉字  可以是字母 和数字,可以代表图片,视频,url,)
		通过一些特定的几何图形,形成黑白向间的图案.图案就是二维码 (本质就是存了数据(0 1))

	
+ 支付
	+ 微信支付的模式(模式二)
	+ 微信支付的解决方案











